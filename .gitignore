node_modules/

# Build output
dist/
playable-snapshot/
perf-candidate/
perf-cand2/

# Capture output — 780 MB of PNGs. Regenerate with tools/baseline.mjs.
# Glob, not just `shots/`: comparison passes write shots2/, shots3/ and so on,
# and those are scratch too.
shots*/

# Agent scratch
demo/
.tmp-demo/
.tmp-rend/
revcrops/
dist-portal/
